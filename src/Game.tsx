import { Button } from "@/components/ui/button"

const Test = () => {
  return (
    <div>
      <Button variant="outline" color="primary">
        Hello World
      </Button>
    </div>
  );
};

export default Test;
